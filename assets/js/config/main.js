define([
    "./backbone",
    "./handlebars",
    "./jquery",
    "./marionette",
    "./underscore"
],function (Backbone, Handlebars, $, Marionette, _) {
    /*
     this file does nothing
     except without this file all configs used in `map`
     are not included into final build
     https://github.com/gruntjs/grunt-contrib-requirejs/issues/85
    */
});